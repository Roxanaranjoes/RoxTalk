import type { NextConfig } from "next"; // This comment clarifies we import the Next.js configuration type for type checking.

const nextConfig: NextConfig = { // This comment states we define the Next.js configuration object.
  reactStrictMode: true, // This comment enables React strict mode for highlighting potential issues.
}; // This comment closes the configuration object.

export default nextConfig; // This comment exports the configuration so Next.js can consume it.
